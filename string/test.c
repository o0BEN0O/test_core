//使用strtok()函数:

#include <stdio.h>   
#include <stdlib.h>  
#include <sys/stat.h>
#include <unistd.h>
 
int main(void)
{
	char *buf="action=upgrade";
	int i=0;
	for(i=0;i<strlen(buf);i++)
 	//printf("%d \n",access("/raymarine/Data/core_app.log",F_OK));
	return 0;
}
